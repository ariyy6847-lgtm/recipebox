# Usage

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python -m recipebox --help
```

## Configure

Create a config file (JSON or YAML) and point the tool at it:

```bash
python -m recipebox run --config config.json
```